import { TestBed } from '@angular/core/testing';
import { DeviceViewComponent } from './device-view.component';

describe('DeviceImgContainerComponent', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            declarations: [ DeviceViewComponent ]
        });
    });

    describe('method ', () => {
        it('should ', () => {
        });
    });

    it('should ', () => {
    });
});
